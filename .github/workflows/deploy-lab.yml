name: Deploy to Lab (Simplified)

on:
  workflow_dispatch:
    inputs:
      target_dc:
        description: "Target DC IP or hostname"
        required: true
        type: string
        default: "20.50.126.169"
      admin_username:
        description: "Local admin username on target VM"
        required: false
        type: string
        default: "azureuser"
      skip_validation:
        description: "Skip pre-promotion checks"
        required: false
        type: boolean
        default: false
  workflow_call:
    inputs:
      target_dc:
        description: "Target DC IP or hostname"
        required: true
        type: string
      admin_username:
        description: "Local admin username on target VM"
        required: false
        type: string
        default: "azureuser"
      skip_validation:
        description: "Skip pre-promotion checks"
        required: false
        type: boolean
        default: false
    secrets:
      DOMAIN_ADMIN_USERNAME:
        required: true
      DOMAIN_ADMIN_PASSWORD:
        required: true
      SAFE_MODE_PASSWORD:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install PowerShell modules
        shell: pwsh
        run: |
          Write-Host "📦 Installing PSWSMan for cross-platform WinRM..." -ForegroundColor Cyan
          Install-Module -Name PSWSMan -Scope CurrentUser -Force -AllowClobber -ErrorAction Stop
          
          Write-Host "📦 Verifying PSWSMan..." -ForegroundColor Cyan
          Import-Module PSWSMan -Force -ErrorAction Stop
          Get-Module PSWSMan

          Write-Host "🔧 Installing WSMan client (needs root)..." -ForegroundColor Cyan
          sudo $(which pwsh) -Command 'Install-Module -Name PSWSMan -Force -AllowClobber; Import-Module PSWSMan; Install-WSMan'

          Write-Host "✅ PowerShell modules installed" -ForegroundColor Green

      - name: Test connectivity
        shell: pwsh
        run: |
          Write-Host "🔍 Testing connectivity to ${{ inputs.target_dc }}..." -ForegroundColor Cyan
          
          try {
            Test-Connection -TargetName "${{ inputs.target_dc }}" -TCPPort 5985 -Count 1 -ErrorAction Stop
            Write-Host "✅ WinRM port 5985 is reachable" -ForegroundColor Green
          } catch {
            Write-Host "❌ Cannot reach WinRM port 5985" -ForegroundColor Red
            exit 1
          }

      - name: Promote to Domain Controller
        shell: pwsh
        env:
          LOCAL_ADMIN_USER: ${{ inputs.admin_username || 'azureuser' }}
          LOCAL_ADMIN_PASS: ${{ secrets.DOMAIN_ADMIN_PASSWORD }}
          DOMAIN_ADMIN_USER: ${{ secrets.DOMAIN_ADMIN_USERNAME }}
          DOMAIN_ADMIN_PASS: ${{ secrets.DOMAIN_ADMIN_PASSWORD }}
          SAFE_MODE_PASS: ${{ secrets.SAFE_MODE_PASSWORD }}
        run: |
          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Cyan
          Write-Host "  DC Promotion - linkedin.local" -ForegroundColor Cyan
          Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Cyan
          Write-Host ""
          Write-Host "Target: ${{ inputs.target_dc }}" -ForegroundColor Yellow
          Write-Host ""

          # Use LOCAL admin to connect to non-domain VM
          $localPassword = ConvertTo-SecureString $env:LOCAL_ADMIN_PASS -AsPlainText -Force
          $localCredential = New-Object System.Management.Automation.PSCredential($env:LOCAL_ADMIN_USER, $localPassword)
          
          # Domain credentials for joining domain
          $domainPassword = ConvertTo-SecureString $env:DOMAIN_ADMIN_PASS -AsPlainText -Force
          $domainCredential = New-Object System.Management.Automation.PSCredential($env:DOMAIN_ADMIN_USER, $domainPassword)
          
          $safeModePassword = ConvertTo-SecureString $env:SAFE_MODE_PASS -AsPlainText -Force

          try {
            Write-Host "📡 Creating remote PowerShell session (using local admin)..." -ForegroundColor Cyan
            $session = New-PSSession -ComputerName "${{ inputs.target_dc }}" -Credential $localCredential -SessionOption (New-PSSessionOption -SkipCACheck -SkipCNCheck)
            Write-Host "✅ Session established" -ForegroundColor Green
            Write-Host ""

            Write-Host "📦 Installing AD DS Role..." -ForegroundColor Cyan
            $roleResult = Invoke-Command -Session $session -ScriptBlock {
              $result = Install-WindowsFeature -Name AD-Domain-Services -IncludeManagementTools
              return $result.Success
            }
            Write-Host "✅ AD DS Role installed (Success: $roleResult)" -ForegroundColor Green
            Write-Host ""

            Write-Host "🚀 Adding DC to existing domain (linkedin.local)..." -ForegroundColor Cyan
            Write-Host "⏳ This will take 10-15 minutes..." -ForegroundColor Yellow
            Write-Host ""
            
            $result = Invoke-Command -Session $session -ArgumentList $safeModePassword, $domainCredential -ScriptBlock {
              param($SafeModePwd, $DomainCred)
              Install-ADDSDomainController `
                -DomainName "linkedin.local" `
                -Credential $DomainCred `
                -InstallDns:$true `
                -SafeModeAdministratorPassword $SafeModePwd `
                -Force:$true `
                -NoRebootOnCompletion:$false `
                -Confirm:$false
            }
            
            if ($result) {
              Write-Host "✅ DC promotion command executed successfully" -ForegroundColor Green
            }

            Write-Host ""
            Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Green
            Write-Host "  ✅ DC PROMOTION INITIATED!" -ForegroundColor Green
            Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Green
            Write-Host ""
            Write-Host "Server is rebooting and completing promotion..." -ForegroundColor Yellow
            Write-Host "Domain: linkedin.local" -ForegroundColor White
            Write-Host "DC: ${{ inputs.target_dc }}" -ForegroundColor White
            Write-Host ""

          } catch {
            Write-Host ""
            Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Red
            Write-Host "  ❌ DC PROMOTION FAILED" -ForegroundColor Red
            Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Red
            Write-Host ""
            Write-Host "Error: $($_.Exception.Message)" -ForegroundColor Red
            Write-Host ""
            exit 1
          } finally {
            if ($session) {
              Remove-PSSession -Session $session -ErrorAction SilentlyContinue
            }
          }
